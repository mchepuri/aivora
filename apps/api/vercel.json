{
  "buildCommand": "cd ../../packages/shared && npx tsc && cd ../../apps/api && npx prisma generate && npm run build",
  "installCommand": "cd ../.. && npm install --include=dev",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "memory": 512
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
